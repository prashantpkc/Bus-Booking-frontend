import { dividerClasses } from '@mui/material';
import axios from 'axios';

const CreateBooking =  () => {
//   const busName = 'busNameHere'; // or some other identifying information
//   const userEmail = 'userEmailHere'; // or some other identifying information

//   // Retrieve the ObjectId for the relevant bus document
//   const busResponse = await axios.get(``);
//   const busId = busResponse.data._id;

//   // Retrieve the ObjectId for the relevant user document
//   const userResponse = await axios.get(``);
//   const userId = userResponse.data._id;

//   // Create the booking using the retrieved ObjectId values
//   const bookingData = {
//     busId,
//     userId,
//     numberOfSeats: 2,
//     date: new Date(),
//     bookingDate: new Date(),
//     totalPrice: 100,
//   };

//   try {
//     const response = await axios.post('/bookings', bookingData);
//     console.log(response.data);
//   } catch (error) {
//     console.error(error);
//   }

return (
    <div>hhii</div>
    )
};

export default CreateBooking